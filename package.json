{
  "name": "theontariorareactiongroup-ca",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev --no-fork",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "sync-secrets": "gh secret set --env-file .env"
  },
  "dependencies": {
    "nodemailer": "^8.0.7",
    "nuxt": "^3.21.4",
    "vue": "^3.5.33",
    "vue-router": "^5.0.6"
  },
  "devDependencies": {
    "@types/nodemailer": "^8.0.0"
  }
}
